const Discord = require("discord.js");
const db = require("quick.db");
module.exports = {
  name: "test",
  category: "info",
  description: "under testing command",
  run: async (client, message, args) => {
  }
};
